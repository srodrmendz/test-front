import { css } from "@emotion/css";

const style = {
    root: css({
        display: "flex",
        padding: "8px",
    }),
    searchBar: css({
        padding: "16px 0",
    }),
};

export default style;
